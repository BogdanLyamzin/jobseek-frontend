import React from 'react';
import CreateOutlinedIcon from '@material-ui/icons/CreateOutlined';

const IconPen = ({ className, click }) => {
	return <CreateOutlinedIcon className={className} onClick={click} />;
};

export default IconPen;
